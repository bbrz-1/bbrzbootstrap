$(document).ready(function() {
  
    let currentUser;

    // Assign User 1 or User 2 dynamically


    assignUser();

    // Load chat history from the server GET
    const loadChatHistory = () => {
        $.ajax({
       
        });
    };

    // Send a new message to the server POST
    const sendMessage = (message, sender) => {
        
    };

    // Load chat history on page load
    loadChatHistory();

    // Send message on button click
    sendBtn.click(function() {
       
    });

    // Send message on Enter key
    

    // Dynamically update chat content every 3 seconds


});
